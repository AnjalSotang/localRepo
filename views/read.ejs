<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h3>Read Operations</h3>

        <div class="row">
            <% rows.forEach((eachData, index) => { %>
                <div class="col-md-4 mb-3">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title"><%- eachData.name %></h5>
                            <h6 class="card-subtitle mb-2 text-body-secondary"><%- eachData.email %></h6>
                            <p class="card-text"><%- eachData.feedback %></p>
                            <p class="card-text"><%- eachData.ID %></p>
                            <a href="/update-data/<%= eachData.id %>"" class="card-link">Edit</a>
                            <a href="/delete-data/<%= eachData.id %>" class="card-link">Delete</a>
                        </div>
                    </div>
                </div>
                <% if ((index + 1) % 3 === 0) { %>
                    </div><div class="row">
                <% } %>
            <% }); %>
        </div>
       
    </div>

    <!-- Bootstrap JS Bundle (includes Popper.js) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
