<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <link rel="stylesheet" href="styles/login.css"> <!-- Your custom CSS file -->
    <style>
        .error-message {
            background-color: #f44336;
            /* Red background */
            color: #fff;
            /* White text color */
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            /* Add a shadow for depth */
        }
    </style>
</head>

<body>
    <div class="container">
        
        <form id="loginForm" action="/auth/login" method="POST">
            <% if (typeof messages !=='undefined' && messages.error) { %>
                <div class="error-message">
                    <p>
                        <%= messages.error %>
                    </p>
                </div>
                <% } %>
    
            <div class="lh">LOG IN</div>
            <div class="signup">
                <h3 style="color:#fff;">New to this site?</h3>
                <a href="/register">Register Now</a>
            </div>

            <div class="input-control">
                <!-- <label for="email" style="color: #fff;">Email</label> -->
                <input id="email" name="email" type="email" placeholder="Enter Your Email" autocomplete="off">
            </div>

            <div class="input-control">
                <!-- <label for="password"style="color: #fff;">Password</label> -->
                <input id="password" name="password" type="password" placeholder="Enter your password" autocomplete="new-password">
            </div>

            <!-- Form submission button -->
            <div class="btn">
                <button type="submit">Log In</button>
                <a href="#">Forgot Password ?</a>
            </div>
        </form>
    </div>
</body>

</html>
